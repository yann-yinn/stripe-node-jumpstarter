{
  "name": "stripe-node-starter",
  "version": "1.2.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "jest ./src/tests",
    "dev": "cross-env NODE_ENV=development nodemon -r dotenv/config src/index.js",
    "build": "cd front && npm install && npm install --only=dev && npm run build && cd -",
    "start": "cross-env NODE_ENV=production node src/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-user-management": "^1.6.5",
    "mongodb": "^3.6.4",
    "nodemon": "^2.0.7",
    "stripe": "^8.137.0"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "localtunnel": "^2.0.1"
  }
}
