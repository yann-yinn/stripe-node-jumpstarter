{
  "name": "stripe-node-starter",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=development nodemon -r dotenv/config src/index.js",
    "build": "cd front && npm install && npm install --only=dev && npm run build && cd -",
    "start": "cross-env NODE_ENV=production node src/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-user-management": "^1.6.3",
    "mobile-detect": "^1.4.4",
    "mongodb": "^3.6.4",
    "nodemon": "^2.0.7",
    "stripe": "^8.137.0"
  },
  "devDependencies": {
    "localtunnel": "^2.0.1",
    "macaddress": "^0.5.1"
  }
}
